#include <iostream>
#include <string>
#include <set>

class solution {
  public:
    std::string originalDigits(std::string & s) {
        std::multiset<char> dict(s.begin(), s.end());
    }
};

int main() {
    
}
